name: gherkin-format

on:
  pull_request:
    paths:
      - 'gherkin/**'
      - '.prettierrc'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/gherkin-format.yml'

permissions:
  contents: read

jobs:
  format-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npm run format:check
