{
  "name": "fm-provider-test-harness",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Gherkin acceptance spec and mock backend for the Dynatrace OpenFeature providers.",
  "scripts": {
    "format": "prettier --write \"gherkin/**/*.feature\"",
    "format:check": "prettier --check \"gherkin/**/*.feature\""
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "prettier-plugin-gherkin": "^4.0.0"
  }
}
